import React from 'react';
import {Navigation} from '../components';

const Menu = () => {
    return (
        <div className='menu'>
            <Navigation/>
        </div>
    );
}

export default Menu